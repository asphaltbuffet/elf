[tools]
"aqua:fsaintjacques/semver-tool" = "latest"
"aqua:golangci/golangci-lint" = "prefix:2.8"
"aqua:gotestyourself/gotestsum" = "latest"
"aqua:rhysd/actionlint" = "latest"
"aqua:vektra/mockery" = "2"
"go:golang.org/x/tools/cmd/stringer" = "latest"

[tasks.actionlint]
description = "lint GitHub Actions workflows"
sources = [".github/workflows/*.yml"]
run = "actionlint .github/workflows/*.yml"

[tasks.build]
description = "go build"
hide = true
run = "go build -o dist/ ./..."

[tasks.ci]
description = "CI build pipeline"
depends = ["actionlint","generate", "mock", "mod-tidy", "test", "cover", "build", "diff"]
hide = true

[tasks.clean]
description = "remove build artifacts"
run = '''
#!/usr/bin/env bash

declare -a dirs=("dist" "bin" "completions" "manpages" "release_notes")

for d in "${dirs[@]}"; do
  rm -rf $d
done

go clean -i -cache -testcache -modcache -fuzzcache -x
'''
hide = true

[tasks.cover]
description="go test coverage"
depends = ["test"]
sources = ["bin/coverage.out"]
outputs = ["bin/coverage.html"]
run = [
  "go tool cover -html=bin/coverage.out -o bin/coverage.html",
  "go tool cover -func=bin/coverage.out"
]

[tasks.dev]
description = "dev build pipeline"
depends = ["generate", "mock", "lint", "test", "snapshot"]

[tasks.diff]
description = "git diff"
run = '''
#!/usr/bin/env bash

git diff --exit-code

RES=$(git status --porcelain)
if [ -n "$RES" ]; then
  echo "$RES" && exit 1
fi
'''
hide = true

[tasks.generate]
description = "auto generate artifacts"
alias = "gen"
sources = ["./**/*.go"]
run = "go generate ./..."

[tasks.lint]
description = "lint project files"
depends = ["generate", "mock"]
sources = ["**/*.go", ".golangci.yml"]
outputs = ["bin/golangci-lint.html"]
run = [
  "mkdir -p bin",
  "golangci-lint run --fix --output.html.path=bin/golangci-lint.html --output.text.path=stdout",
]

[tasks.manpages]
description = "generate man pages"
sources = ["cmd/**/*.go"]
outputs = ["manpages/*"]
run = [
  "rm -rf manpages",
  "mkdir manpages",
  "go run . man | gzip -c -9 >manpages/elf.1.gz",
]

[tasks.mock]
description = "generate mocks"
depends = ["generate"]
run = "mockery --log-level=warn"

[tasks.mod-tidy]
description = "go mod tidy"
alias = "mod"
sources = ["**/*.go", "**/go.mod", "**/go.sum"]
outputs = ["**/go.mod", "**/go.sum"]
run = "go mod tidy"
depends_post = ["nix-hash"]

[tasks.nix-hash]
description = "Update flake.nix vendorHash"
run = '''
#!/usr/bin/env bash
set -euo pipefail

# Build and capture the expected hash from error output
OUTPUT=$(nix build .#default 2>&1 || true)

# Extract the "got:" hash from the error message
NEW_HASH=$(echo "$OUTPUT" | grep -oP 'got:\s+\Ksha256-[A-Za-z0-9+/=]+' | head -1 || true)

if [ -z "$NEW_HASH" ]; then
  echo "Build succeeded or no hash found - vendorHash is up to date"
  exit 0
fi

echo "Updating vendorHash to: $NEW_HASH"

# Update the flake.nix file
sed -i "s|vendorHash = \"sha256-[^\"]*\"|vendorHash = \"$NEW_HASH\"|" flake.nix

echo "Updated flake.nix - run 'nix build' to verify"
'''

[tasks.snapshot]
description = "goreleaser build snapshot"
depends = ["generate", "mod-tidy"]
alias = "ss"
run = [
  "goreleaser check -q",
  "goreleaser build --clean --single-target --snapshot",
]

[tasks.test]
description = "run test suite"
depends = ["generate", "mock"]
sources = ["**/*.go", "**/testdata/**"]
outputs = ["bin/coverage.out"]
run = [
  "mkdir -p bin",
  "gotestsum -- -race -covermode=atomic -coverprofile=bin/coverage.out $(go list ./... | grep -v /mocks)",
]
