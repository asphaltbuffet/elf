#!/usr/bin/env bash
# Solution for Advent of Code {{ .Year }} day {{ .Day }}.
#
# Each part reads the puzzle input on stdin and echoes the answer on stdout.
# Define part_one and part_two; the elf bash runner sources this file and
# dispatches to them.

part_one() {
  local input
  input="$(cat)"
  echo "part 1 not implemented" >&2
  return 1
}

part_two() {
  local input
  input="$(cat)"
  echo "part 2 not implemented" >&2
  return 1
}
