// Solution for Project Euler problem {{ .Number }}: {{ .Title }}.
//
// Project Euler problems have a single answer. Implement part_one. part_two
// exists only to satisfy elf's shared runner harness and is never invoked.

pub fn part_one(input: &str) -> String {
    let _ = input;
    "problem {{ .Number }} not implemented".to_string()
}

pub fn part_two(_input: &str) -> String {
    "project euler problems have a single part".to_string()
}
